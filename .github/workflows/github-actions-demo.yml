name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - запустить: echo "🔎 Имя вашей ветки - ${{ github.ref }}, а ваш репозиторий - ${{ github.repository }}."
      - имя: Проверить код репозитория
        использует: действия/checkout@v4
      - запустить: echo "💡 Репозиторий ${{ github.repository }} был клонирован в бегуне."
      - запустить: echo "🖥️ Теперь рабочий процесс готов к тестированию вашего кода на бегуне."
      - имя: Список файлов в репозитории
        запустить: |
          ls ${{ github.workspace }}
      - запустить: echo "🍏 Статус этой работы: ${{ job.status }}."
